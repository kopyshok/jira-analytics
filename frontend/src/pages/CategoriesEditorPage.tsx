import { CategoryConfigTab } from './SyncPage';

/**
 * Отдельная страница для категоризации задач.
 * Содержимое — CategoryConfigTab из SyncPage, доступный на /categories.
 */
export default function CategoriesEditorPage() {
  return <CategoryConfigTab />;
}
